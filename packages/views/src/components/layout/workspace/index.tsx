import { Box } from '@mui/material';
import { FC } from 'react';

const Workspace: FC = () => {
  return (
    <Box p={1} height="100%" bgcolor="background.200">
      Workspace works!
    </Box>
  );
};

export default Workspace;
